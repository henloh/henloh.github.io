<c:FragmentDefinition 
    xmlns="sap.m" 
    xmlns:l="sap.ui.layout" 
    xmlns:c="sap.ui.core"
	controllerName="de.henloh.prodts.controller.ProductionLine"> <!-- This part is redundant -->
	
    <Panel
        headerText="{Name}">
        <Select items="{factoryOptions}" 
            change="selectFactory"
            ><c:Item text="{Name}" /></Select>
        
    </Panel>
</c:FragmentDefinition>